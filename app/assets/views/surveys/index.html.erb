<h1>Listing surveys</h1>

<table>
  <tr>
    <th>Ice cream</th>
    <th>Superhero</th>
    <th>Movie star</th>
    <th>World end</th>
    <th>Super bowl</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @surveys.each do |survey| %>
  <tr>
    <td><%= survey.ice_cream %></td>
    <td><%= survey.superhero %></td>
    <td><%= survey.movie_star %></td>
    <td><%= survey.world_end %></td>
    <td><%= survey.super_bowl %></td>
    <td><%= link_to 'Show', survey %></td>
    <td><%= link_to 'Edit', edit_survey_path(survey) %></td>
    <td><%= link_to 'Destroy', survey, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Survey', new_survey_path %>
